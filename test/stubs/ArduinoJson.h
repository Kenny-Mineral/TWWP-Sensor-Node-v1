#pragma once
// Minimal ArduinoJson stub for native unit tests.
// Only declares the types referenced by src headers; no real serialisation.
#include <stddef.h>

struct JsonDocument {
    // Stub — tests that need real JSON should use a proper ArduinoJson build.
};
